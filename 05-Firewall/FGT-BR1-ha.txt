! ============================================================
! FortiGate BR1 — High Availability Configuration
! ============================================================

config system ha
    set mode a-p
    set group-name "BR1-FGT-HA"
    set password ENC AK1...
    set priority 200
    set override enable
    set heartbeat-interface "port3"
    set hbdev "port3" 50
    set monitor "WAN_ORANGE" "WAN_WE" "BR1-LAN-SW"
end
